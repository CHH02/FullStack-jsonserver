{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port ${PORT:-3001} --host 0.0.0.0 --static ./"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.5"
  }
}
